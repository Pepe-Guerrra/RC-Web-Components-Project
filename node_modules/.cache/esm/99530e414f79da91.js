let addFolder,inquirer,color;_4f7‍.x([["validationsComponent",()=>validationsComponent]]);_4f7‍.w("./createStructure.js",[["addFolder",["addFolder"],function(v){addFolder=v}]]);_4f7‍.w("inquirer",[["default",["inquirer"],function(v){inquirer=v}]]);_4f7‍.w("../supportFeature/spin.js",[["color",["color"],function(v){color=v}]]);



       async function validationsComponent(options) {
  options = await promptForMissingOptions(options);
  let isValid = validComponentName(options.name);
  if (isValid) {
    addFolder(options)
  }
}

const errorMsg = color.red(`
  SYNTAX ERROR:
  
  The correct syntax must be in camel case format
  example:
          =>  rc c componentName
                        or
          =>  rc component componentName
`);

async function promptForMissingOptions(options) {
  const question = [];

  if (!options.name) {
    question.push({
      type: 'input',
      name: 'componentName',
      message: "What's your component name",
      default:'myComponent',
    });
  };

  const answer = await inquirer.prompt(question);
  return {
    ...options,
    name: options.name || answer.componentName
  }
};

function validComponentName(name){
  let strgTest = /^([a-z]+)(([A-Z]([a-z]+))+)$/.test(name)
  if (!strgTest) {
    _4f7‍.g.console.log(errorMsg);
    return false;
  };
  return true
};
